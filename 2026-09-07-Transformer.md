# Transformer 学习笔记

> 记录时间：2026-09-07  
> 学习目标：理解 Transformer 的核心结构，为 AI 产品经理技术理解打基础


## 01 早期语言处理技术演进

### N-gram 语言模型

**基本原理：**  
基于词语组合的统计，预测下一个词。

**局限性：**  
- 词语组合数量庞大，无法穷举  
- 无法理解新词汇或低频词汇组合  


### 解决方向：词向量

将词语映射为高维空间中的坐标，每个维度代表一种语义特征。  
例如描述一种动物，可以从“飞行能力”“适合当宠物程度”等维度表示。

通过余弦相似度判断词语之间的语义相近程度。  
词向量能在数学上表示大量维度，帮助模型理解词汇含义。


### 前馈神经网络

**改进点：**  
利用词向量作为输入，训练模型学习语言规律，一定程度解决了对新词汇组合的理解问题。

**局限：**  
只能在固定窗口内处理信息，长句子中会丢失较远位置的信息。


### 循环神经网络 RNN

**核心机制：**  
隐藏状态。模型每读取一个 token，就将前面的信息压缩进隐藏状态，理论上可以持续传递。

**长期依赖问题：**  
较早的信息会被新的信息逐渐覆盖，距离越远信息越模糊，容易产生梯度消失。


### 长短期记忆网络 LSTM

**改进机制：**  
引入“门”结构：

- 输入门：控制哪些新信息写入  
- 遗忘门：决定哪些旧信息需要丢弃  
- 输出门：决定输出哪些信息用于当前预测  

**效果：**  
模型可以学习该记住什么、该忘记什么。

**仍然存在的局限：**  
只能按顺序逐个处理 token，无法并行计算。


## 02 自注意力机制

### 基本原理

像人类阅读一样，让一个词直接关注到与它强相关的词，而不是依赖距离远近。

核心思想：直接建立词与词之间的联系，解决长距离依赖问题。


### QKV 机制

- **Query（查询）**：表达“我想找什么”  
- **Key（键）**：声明“我能提供什么”  
- **Value（值）**：保存实际的信息内容  


### 权重计算过程

#### 1. 点积计算匹配度

用当前 token 的 Query 与所有 token 的 Key 做点积。  
点积结果越大，说明 Query 与 Key 的匹配程度越高。

#### 2. Softmax 转换权重

将匹配分数转换为概率分布，确定每个 token 应该被关注的程度。


### 模型关键组件

#### 多头注意力机制

词与词之间可能存在多种关系类型。  
每个注意力头关注一种关系模式，从不同角度捕捉信息。

多个头并行计算，提升模型表达能力。


#### 位置编码

自注意力机制本身不感知词序。  
位置编码将位置信息注入词向量，使模型能够保留先后顺序。


#### 前馈神经网络 FFN

不关注其他 token，只对单个 token 进行特征变换。  
自注意力负责词与词之间的信息交流，FFN 负责对聚合后的信息进行深度加工。


#### 残差连接结构

**作用：**  
缓解梯度消失问题。

**原理：**  
在输入和输出之间建立一条直接通道：  

`输出 = 输入 + 层的变换结果`

反向传播时，梯度可以通过这条通道直接流回前面的层，不需要经过过多变换，从而避免梯度过度衰减。


## 03 编码器与解码器架构

### 编码器

**处理流程：**  
- 自注意力查看全文  
- 融合上下文信息  
- 输出每个 token 的向量表示  

**职责：**  
只负责理解输入，不生成新的 token。


### 解码器

**生成逻辑：**

#### 1. 掩码自注意力

只允许看到当前及之前生成的 token，防止“偷看”后面的内容。

#### 2. 交叉注意力

用解码器当前的状态作为 Query，去编码器输出中查找相关信息。

#### 3. 最终预测输出

对词表进行打分，通过 Softmax 转换为概率，逐 token 生成文本。

实际生成时可能使用温度采样或 top-p 等方法，不总是选择概率最高的词。


## 04 整体流程总结

1. 输入：词向量 + 位置编码  
2. 自注意力：让每个词关注其他相关词  
3. 多头 + 残差 + 层归一化：多角度捕捉关系，保持信息流动稳定  
4. 前馈神经网络：对每个 token 做深度加工  
5. 堆叠多层：底层学习语法，高层学习语义  
6. 编码器输出：每个词携带全文信息  
7. 解码器：结合已生成内容与编码器输出，逐 token 生成结果


## 05 补充：什么是非线性变换

非线性变换，简单说就是：  
输入和输出之间不是一条直线关系。

### 为什么需要非线性

如果模型中只有线性变换，那么无论堆叠多少层，整体仍然是线性变换，表达能力非常有限。

很多现实问题不是线性可分的，例如：

- “温度越高，冰淇淋销量越高”，这接近线性关系  
- “温度过高时，人们反而不出门，销量下降”，这就不是单纯线性关系

模型需要非线性能力，才能学习复杂的语言规律。

### 常见非线性函数

- ReLU  
- GELU  
- Sigmoid  
- Tanh  

Transformer 中前馈神经网络通常会接一个非线性激活函数，让模型具备更强的拟合能力。


## 06 当前疑问与后续学习方向

- [ ] 残差连接与层归一化的协作关系  
- [ ] 多头注意力的具体维度切分方式  
- [ ] 训练阶段的 Teacher Forcing 机制  
- [ ] 推理阶段的采样策略对比  


> 说明：本笔记用于个人学习记录，部分内容基于个人理解整理，后续会持续修正。
